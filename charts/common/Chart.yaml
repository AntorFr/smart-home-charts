apiVersion: v2
name: common
description: Function library for k8s-at-home charts
type: library
version: 4.10.0
kubeVersion: ">=1.21.0-0"
keywords:
  - k8s-at-home
  - common
home: https://github.com/k8s-at-home/library-charts/tree/main/stable/common
maintainers:
  - name: bjw-s
    email: me@bjw-s.dev
  - name: onedr0p
    email: devin.kray@gmail.com
  - name: dirtycajunrice
    email: nick@cajun.pro
  - name: AntorFR
annotations:
  artifacthub.io/changes: |-
    - kind: added
      description: "`externalSecrets.groups:` — split secrets into one ExternalSecret per consumer (`<fullname>-<group>-secrets`), auto-wired into the matching `additionalContainers` entry. Confines ESO's atomic-sync blast radius: a missing key only fails its own group, not the whole pod's Secret."
